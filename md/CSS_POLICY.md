# CSS Policy — lib_bejson_html v3.0

> Standard component naming, conventions, and best practices for all HTML generated by `lib_bejson_html.py` and `lib_bejson_html_skeletons.py`.

---

## Component Registry

### Top Bar (fixed, always present)

| Class | Purpose |
|---|---|
| `.top-bar` | Fixed black header row (40px), `position: fixed; top: 0`, `z-index: 1000` |
| `.top-bar__toggle` | Menu button (☰), no border, white text |
| `.top-bar__brand` | Page title text in top bar, 13px, white, letter-spaced |

### Sidebar Navigation

| Class | Purpose |
|---|---|
| `.sidebar` | Slide-out nav panel, solid black, 220px, starts below top bar (40px) |
| `.sidebar a` | Nav links, white text, left border indicator on hover/active |
| `.sidebar-overlay` | Semi-transparent backdrop, closes sidebar on tap/click |

### Layout

| Class | Purpose |
|---|---|
| `.scroll-container` | Outer wrapper, `overflow-x: auto`, touch-optimized |
| `.container` | Centered content, max-width 880px, tight padding |
| `main#content` | Semantic landmark, `role="main"` |

### Page Header (content area, NOT top bar)

| Class | Purpose |
|---|---|
| `.page-header` | Seamless content header, no border, spacing only |
| `.page-header h1` | 18px, 600 weight, tight letter-spacing |
| `.page-header .page-url` | Muted link with bottom border |
| `.page-url:hover` | Darker text, darker border |

### Stats Bar

| Class | Purpose |
|---|---|
| `.stats` | Glass-morphism info bar, `backdrop-filter: blur(8px)`, transparent bg |
| `.stats span` | Bold values in primary text color |

### Data Table

| Class | Purpose |
|---|---|
| `.data-table` | Sortable table, `role="table"`, rounded, subtle shadow |
| `.data-table thead th` | Sticky, uppercase, 12px, black bg, white text, sort arrow via `::after` |
| `.data-table tbody td` | Tight padding (5px 10px), row hover effect |
| `.null-val` | Muted italic "null" |
| `.bool-true` / `.bool-false` | Green / red boolean indicators |
| `.json-val` | Monospace code block for JSON/array values, subtle bg |

### Card Grid

| Class | Purpose |
|---|---|
| `.card-grid` | Responsive grid, `repeat(auto-fill, minmax(170px, 1fr))`, 8px gap |
| `.card` | Individual card, left accent border, hover lift + shadow |
| `.card h3` / `.card h3 a` | Card title, dark text, accent on hover |
| `.card .count` | Item count subtitle, muted |
| `.card .subtitle` | Optional subtitle, muted |

### Section Titles

| Class | Purpose |
|---|---|
| `.section-title` | 15px, 600 weight, bottom border separator |

### Badges

| Class | Purpose |
|---|---|
| `.badge` | Base badge, uppercase, 10px, rounded |
| `.badge-active` | Green badge for active items |
| `.badge-archived` | Red badge for archived items |

### Back Link

| Class | Purpose |
|---|---|
| `.back-link` | "← Back" navigation link, muted color |

### Feed

| Class | Purpose |
|---|---|
| `.feed-item` | Feed entry card, left accent border, hover slide |
| `.feed-item h3 a` | Feed title, dark text, accent on hover |
| `.feed-meta` | Date/author line, muted |
| `.feed-body` | Feed content text |
| `.feed-tag` | Small uppercase tags inside feed items |

### Log / Tabled Log

| Class | Purpose |
|---|---|
| `.log-header` | Log page header |
| `.log-entry` | Individual log record, hover bg |
| `.log-line` | Timestamp + entity + index row |
| `.log-ts` | Timestamp column, muted |
| `.log-entity` | Entity badge, accent color |
| `.log-idx` | Record index number |
| `.log-field-row` | Indented field list with left border |
| `.log-field` | Single field=value pair |
| `.log-field-name` | Field name, muted |
| `.log-field-sep` | Equals separator |
| `.log-field-val` | Field value (with `.null`, `.bool-true`, `.bool-false` modifiers) |
| `.filter-bar` | Search/filter input row |

### Code / Source Display

| Class | Purpose |
|---|---|
| `.code-block` | Syntax-highlighted code block, monospace, shadow on hover |
| `.code-header` | Code block title bar with language label |
| `.code-copy` | Copy button, transitions to accent on hover |

### Media Embeds

| Class | Purpose |
|---|---|
| `.media-embed` | Responsive 16:9 video embed (iframe/video/img) |
| `.media-thumb` | Image thumbnail with hover scale effect |
| `.media-caption` | Caption text below media |

### Footer

| Class | Purpose |
|---|---|
| `.site-footer` | Footer landmark, `role="contentinfo"`, top border, centered |
| `.site-footer a` | Muted links, accent on hover |

---

## BEM Naming Convention

All components follow **BEM** (Block\_\_Element--Modifier):

```
Block:       .card, .sidebar, .top-bar, .feed-item
Element:     .top-bar__toggle, .top-bar__brand, .card__subtitle
Modifier:    .badge--active → .badge-active (flat variant)
```

**Rules:**
- Double underscore `__` separates block from element
- Double hyphen `--` for modifiers (flattened to single `-` for readability)
- No nested element selectors in CSS (`.card h3` OK, `.card__title` preferred for new components)

---

## CSS Variables (Color Palette)

### Light Theme (default)

| Variable | Value |
|---|---|
| `--bg` | `#ffffff` |
| `--surface` | `#ffffff` |
| `--border` | `#e8e8e8` |
| `--text` | `#0a0a0a` |
| `--text_mut` | `#6b7280` |
| `--accent` | `#de2626` |
| `--accent_h` | `#b91c1c` |
| `--row_hover` | `#f9fafb` |
| `--glass_bg` | `rgba(255,255,255,0.7)` |
| `--glass_border` | `rgba(0,0,0,0.08)` |

### Dark Theme

| Variable | Value |
|---|---|
| `--bg` | `#0a0a0a` |
| `--surface` | `#111111` |
| `--border` | `#1f1f1f` |
| `--text` | `#f0f0f0` |
| `--text_mut` | `#6b7280` |
| `--accent` | `#de2626` |
| `--accent_h` | `#ef4444` |
| `--row_hover` | `#161616` |
| `--glass_bg` | `rgba(17,17,17,0.8)` |
| `--glass_border` | `rgba(255,255,255,0.06)` |

---

## Fonts

| Usage | Font Stack |
|---|---|
| Body text | `'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif` |
| Code / monospace | `'Roboto Mono', 'Source Code Pro', 'SF Mono', 'Cascadia Code', 'Fira Code', monospace` |

Loaded via Google Fonts in `HTML_SKELETON`:
```html
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Roboto+Mono:wght@400;500&family=Source+Code+Pro:wght@400;500&display=swap" rel="stylesheet">
```

---

## Responsive Breakpoints

Mobile-first approach, single breakpoint at `640px`:

```css
@media (max-width: 640px) {
    /* Stack grids to single column */
    /* Tighter table padding */
    /* Full-width filter bar */
    /* Smaller top bar brand text */
}
```

---

## Best Practices

1. **Use semantic HTML**: `<main role="main">`, `<nav role="navigation">`, `<article>`, `<section>`, `<footer role="contentinfo">`
2. **Low specificity**: Class-only selectors, no ID styling
3. **Transitions everywhere**: 0.12–0.15s ease on all interactive elements
4. **Glass effects**: `backdrop-filter: blur(8px)` on stats/info bars
5. **Hover micro-interactions**: Cards lift (`translateY(-1px)`), feed items slide (`translateX(2px)`)
6. **Accessibility**: `aria-label` on nav landmarks, `aria-expanded` on toggle button
7. **Top bar offset**: `body {{ padding-top: 40px; }}` always matches `.top-bar` height

---

## Deprecated / Removed Classes

| Old Class | New Class | Status |
|---|---|---|
| `.hamburger` | `.top-bar__toggle` | Removed |
| `.nav-overlay` | `.sidebar-overlay` | Removed |
| `.site-header` | `.page-header` | Removed |
| `.site-link` | `.page-url` | Removed |
| `.site-footer` (old) | `.site-footer` (new, `role="contentinfo"`) | Updated |
| `.type-grid` | `.card-grid` | Removed |
| `.type-card` | `.card` | Removed |
| `<table>` (plain) | `<table class="data-table">` | Updated |
